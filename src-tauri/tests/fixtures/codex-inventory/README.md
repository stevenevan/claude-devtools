# Codex inventory fixture

This tree is disposable live-QA input. Copy it to a temporary directory and
set `CODEX_HOME` to that directory before starting the app. It contains only
text metadata and must never be used as a real Codex home.

